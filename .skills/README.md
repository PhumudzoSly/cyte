# Local Skills Directory

Place local skills under this folder.

Each skill should be structured as:

```text
.skills/
  <skill-name>/
    SKILL.md
    references/   (optional)
    assets/       (optional)
```

Also register every skill in the repository root `SKILLS.md`.
